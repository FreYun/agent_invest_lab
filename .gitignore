__pycache__/
*.pyc
.pytest_cache/
*.log
# dashboard "新建回测" 把 world.yaml 的副本写到 world/config/world-tmp-*.yaml
# （写在同目录是为了让 calendar / fund_mcp_cli 等相对路径还能解析）
world/config/world-tmp-*.yaml

# runtime artifacts
.playwright-mcp/
bots/*/memory/
data/
session/
*.bak

# Python virtualenv (可由 requirements.txt / pyproject.toml + uv.lock 重建)
.venv/
**/.venv/
venv/

# git worktrees
.claude/worktrees/

# symlink to ../.openclaw (separate git repo, for read/edit convenience only)
/.openclaw

# debug screenshots
*.png

# fund-portfolio runtime db
fund-portfolio-mcp/fund.db

# research backtest artifacts (generated equity curves / run cards / result csvs)
research/**/runs/
research/**/*.csv


.venv
# 运行时日志目录（*.log 之外的轮转/结构化产物也不入库）
/logs/

# 临时研发素材目录（demo 过程稿 / ppt 渲染 / 资产配置草稿，不入库）
/temp/

# 根目录调试输出与 demo 研发过程产物，不入库
/seg.txt
/daily_factor_values.csv
/factor_definition.csv

# 第三方 vendor（research-loop-105d 的 rust target 编译产物 + workspace/tmp，2.6G 构建产物，不入库）
/vendor/

# dashboard 临时配置的备份 / 隐藏中间产物（.kimibak 备份、.world-*.intraday.* 隐藏临时配置）
world/config/*.kimibak
world/config/.world-*.yaml

# 本地临时产物和敏感联调草稿，不入库
/.write_test_xyz
/runtime/res-reports-backfill-cache/
/world/tmp_analyze*.py
/docs/申赎*.py

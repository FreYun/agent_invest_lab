# 字段映射对照(我方原始字段 → 对方口径)

> 对方口径 CSV(`data/orders_mapped.csv`、`data/daily_nav_mapped.csv`)即按本表从我方原始表转出。
> 原始口径文件保留在 `data/raw/` 供溯源核对。

## 一、订单流水 `data/orders_mapped.csv`

| 对方口径列 | 我方原始列 | 取值 / 规则 | 待确认 |
|---|---|---|---|
| `customer_no` | `bot_id` | bot_id→客户号映射(本样本 = `SIMACCT0001`) | ⚠ 客户号规则 |
| `sub_account_no` | (无) | 默认 = `customer_no` | ⚠ 我方无真实子账户 |
| `portfolio_id` | `(bot_id, order_run_id)` | `customer_no + '_' + run_id` | ⚠ 组合号规则 |
| `portfolio_name` | `bot_id` | `bot102_多基金配置` | ⚠ 组合命名 |
| `fund_code` | `fund_code` | 原样 6 位 | |
| `fund_name` | `fund_name` | 原样 | |
| `C_BUSINTYPE` | `order_type` | **buy→22 / sell→24** | ⚠ 业务码枚举 |
| `busin_type_desc` | `order_type` | 申购 / 赎回(中文便于核对) | |
| `apply_date` | `order_date` | 申请日(T) | |
| `confirm_date` | `confirm_date` | 确认日(T+1) | |
| `apply_amount` | `order_amount`(仅 buy) | **申购金额(元)**;赎回行留空 | ★ 拆解语义重载 |
| `apply_vol` | `order_amount`(仅 sell) | **赎回份额(份)**;申购行留空 | ★ 拆解语义重载 |
| `confirm_nav` | `confirm_nav` | 确认净值(PIT 收盘) | |
| `confirm_vol` | `confirmed_shares` | 确认份额 | |
| `confirm_amount` | `confirmed_amount`(仅 sell) | 赎回到账金额;申购留空(净申购额 = `apply_amount − fee`) | |
| `fee` | `fee` | 手续费(申购费 + 赎回费**都在此列**) | |
| `status` | `status` | 恒 `confirmed` | |
| `src_run_id` | `order_run_id` | 溯源用,对方可删 | |

> **关键改进**:我方原始 `order_amount` 在 buy/sell 含义不同(买=金额、卖=份额),易踩坑。
> 对方口径已拆成 `apply_amount`(申购金额)/ `apply_vol`(赎回份额)两列,各取所需,不再重载。

## 二、组合每日净值 `data/daily_nav_mapped.csv`

| 对方口径列 | 我方原始列 | 说明 |
|---|---|---|
| `customer_no` / `portfolio_id` | — | 同上映射,可与订单表 join |
| `trade_date` | `trade_date` | 交易日 |
| `net_value` | `net_value` | 组合单位净值,初始 1.0000 |
| `total_asset` | `total_value` | 组合总资产(元) |
| `market_value` | `invested_value` | 持仓基金市值(元) |
| `cash` | `cash` | 现金(元) |
| `cash_weight` | `cash_weight` | 现金占比(可靠) |
| `noncash_ratio` | 派生 | = `market_value / total_asset`,**替代不可用的股/债/金权重** |
| `cum_return_pct` | `cumulative_return_pct` | 累计收益 % |
| `max_drawdown_pct` | `max_drawdown_pct` | 最大回撤 % |

> 勾稽:`total_asset = market_value + cash`;`net_value = total_asset / 1,000,000`(初始本金)。
> **未提供 equity/bond/gold 三类权重**:该多基金组合持仓 `asset_class` 未落库,三列恒 0,故剔除,改用 `noncash_ratio`。

## 三、待对方确认(4 项)

1. **`C_BUSINTYPE` 枚举**:现按「申购 = 22 / 赎回 = 24」映射(对方截图所推断),请确认贵方系统实际码值。
2. **`customer_no` / `sub_account_no` 生成规则**:我方为模拟 Agent,无真实客户号/子账户;现用 `SIMACCT` 合成、子账户默认同客户号——请贵方给定命名规则后即可批量重映射。
3. **`portfolio_id` 规则**:现用 `customer_no + '_' + run_id`(组合 = `(bot_id, run_id)`)。
4. **是否需要更多字段**:若贵方目标表还有必填项(交易所代码 / 币种 / TA 代码 / 份额类别等),**请提供完整导入模板或表结构(DDL)**,我方按模板精确对齐再出全量。

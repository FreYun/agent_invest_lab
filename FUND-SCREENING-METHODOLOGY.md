# 权益基金筛选方法论（业绩 · 规模 · 费率）

这是一份**独立的基金筛选框架**：它只回答一个问题——**给定一批权益类候选基金，怎么把它们筛出优劣排序**。它不管市场择时、不管主线识别、不管组合配置（那是 `METHODOLOGY.md` 的事）。本框架的输入是一个候选池，输出是「出局名单 + 入围基金的三维评分与综合排序」。

## 适用范围与边界

- **只筛权益类**：主动权益基金（普通股票型、偏股混合型）+ 股票指数基金（含 ETF 联接、增强指数）。债券基金、货基、QDII 债、商品基金不适用本口径——它们的业绩/规模/费率基准完全不同。
- **筛选 ≠ 择时**：本框架判断的是"这只基金本身好不好"，不是"现在该不该买它对应的赛道"。一只通过筛选的优质科技基金，在科技主线走坏时一样不该重仓——那是择时框架的判断。
- **主动 vs 指数分开给口径**：同样是"规模""费率"，主动权益和指数基金的合理区间不同（指数基金规模上限更宽、费率天然更低、不看选股 alpha 只看跟踪），下文凡有差异处都分别注明。

## 数据边界（只用真实存在的工具）

筛选只能建立在当前 `simworld` 确实有的 PIT 数据上：

| 维度 | 主用工具 | 取什么字段 |
|---|---|---|
| 业绩 | `fund_performance` / `get_fund_detail` | 多周期 `return_pct` / `max_drawdown_pct` / `sharpe_ratio` / `calmar` / `rank_pct` 同类排名（`rank_text` "3274/3745" 格式） |
| 规模 | `fund_basic_info` / `get_fund_detail` | `scale`（亿元）、成立时间（`estabdate`） |
| 费率 | `fund_rate` | 管理费 + 托管费（年持有成本）、申购费、赎回费（含 7 日惩罚档） |
| 辅助·经理 | `fund_manager_profile` | 现任经理、任职区间（任职年限） |
| 辅助·风格 | `fund_style_analysis` / `get_fund_detail` | 大小盘 × 成长价值、行业暴露（判断风格是否漂移） |

**净值工具调用红线**：`fund_nav` 原则上不主动调用——业绩用 `fund_performance` / `get_fund_detail` 的派生区间即可，不要为筛选去批量拉净值序列自己算收益。

---

## 整体流程：硬门槛排除 → 三维加权打分 → 排序入围

筛选分两段，顺序固定：

1. **第一段·硬门槛（一票否决）**：任一不达标直接出局，不进入打分。门槛是为了剔除"结构性不合格"的基金（要清盘的、没历史的、长期垫底的），不是为了排优劣。
2. **第二段·三维加权打分（0–100）**：对通过门槛的基金，按 **业绩 50% / 规模 30% / 费率 20%** 合成综合分，排序取前几名。辅助维度（经理/风格）不进主分，作为打分微调与最终复核。

> 为什么是漏斗而不是纯打分：纯打分会让一只规模 2 亿、短期夏普虚高的"伪强基"靠业绩分混进来。先用硬门槛卡掉结构性风险，再让打分在"合格池"里排序，才不会被极端值带偏。

---

## 第一段：硬门槛（任一不达标即出局）

| # | 门槛 | 主动权益 | 指数基金 | 理由 |
|---|---|---|---|---|
| 1 | **成立时间** | ≥ 2 年 | ≥ 2 年 | 没走过一轮完整波动的基金，业绩无参考价值；< 2 年只能"观察位"不进筛选 |
| 2 | **规模下限** | ≥ 2 亿 | ≥ 1 亿 | 低于此线有清盘风险（连续 60 日 < 5000 万 或持有人过少会触发清盘） |
| 3 | **规模上限** | ≤ 200 亿 | 不设硬上限（>300 亿降级处理） | 主动权益规模过大，选股 alpha 被稀释、调仓冲击成本放大；指数基金大反而更稳 |
| 4 | **长期排名** | `3y`（或成立以来）`rank_pct` 不在末 30% | 跟踪误差不在同类末 30% | 长期垫底的不洗——主动看排名，指数看跟踪误差 |
| 5 | **现任经理任职** | ≥ 1 年 | （指数基金弱化此项） | 历史业绩不属于现任经理的，不能直接采信；刚换帅 < 1 年 → 降级到"观察位" |

**出局即终止**：被任一门槛淘汰的基金不再进入第二段，直接记入"出局名单"并注明触发了哪条门槛。

---

## 第二段：三维加权打分

每维独立打 0–100，再按权重合成：**综合分 = 业绩×0.5 + 规模×0.3 + 费率×0.2**。

### ① 业绩（权重 50%）——重长周期、重风险调整、重排名一致性

业绩是核心，但**绝不只看绝对收益、绝不只看短窗**。打分看四件事：

1. **长周期优先（最重）**：`3y / 5y / since_inception` 的 `return_pct` 权重 > `1y` > `3m / 1m`。
   - 现实里 `1y +30%` 的基金 `5y` 可能是 `-40%`——短窗看不出"医药十年熊 / 半导体十年牛"这种结构性事实。短期亮眼、长期平庸的基金，业绩分要压下来。
2. **风险调整后看，不只看收益**：同看 `sharpe_ratio` 与 `calmar`（收益/最大回撤）。两只收益相近时，夏普/卡玛高的得分高——它用更小的波动拿到同样的钱。
3. **回撤**：`max_drawdown_pct` 越小越好；长周期 MDD 反映这只基金在熊市里真实掉了多少。
4. **排名一致性**：多个周期 `rank_pct` 都在同类前 30% > 单期暴涨进前 5%。一致排名靠前 = 可复制的能力；单期暴涨 = 大概率昙花一现或押对单一风格。

**业绩分档（参考刻度）**：

| 档 | 特征 | 分区间 |
|---|---|---|
| 优 | 3y/5y `rank_pct` 均在前 20%，长期夏普 ≥ 1，长期 MDD 同类偏低 | 80–100 |
| 良 | 长周期排名前 30%–40%，夏普中上，回撤可控 | 60–80 |
| 中 | 排名中游，靠短期窗口拉动，长期平庸 | 40–60 |
| 差 | 仅短期亮眼、长期靠后，或回撤显著大于同类 | < 40 |

> 指数基金的"业绩"主要看**跟踪误差 + 超额（增强型）**，而非选股 alpha：跟踪误差小、增强型有稳定正超额 → 高分；跟踪误差大、超额不稳 → 降分。

### ② 规模（权重 30%）——甜区满分，两头扣分

规模不是越大越好，也不是越小越灵活。打分用**甜区模型**：

| 规模区间（主动权益） | 含义 | 分区间 |
|---|---|---|
| **5 亿 – 100 亿** | 甜区：流动性足、无清盘风险、alpha 未被稀释 | 85–100 |
| 2 亿 – 5 亿 | 偏小：流动性/清盘风险抬头，但仍可参与 | 60–85 |
| 100 亿 – 200 亿 | 偏大：调仓冲击成本上升、alpha 稀释 | 50–80 |
| （> 200 亿已被门槛淘汰） | — | — |

补充扣分项：
- **规模趋势**：连续多季持续缩水（净赎回）→ 减分，可能预示资金用脚投票或临近清盘。
- 指数基金甜区上移：**10 亿 – 500 亿** 满分区，规模越大跟踪越稳、流动性越好，上限可放宽。

### ③ 费率（权重 20%）——确定性的负 alpha，作决胜项

费率差异通常只有零点几个百分点，远小于业绩/规模能拉开的几十个百分点——所以权重最低。但费率是**唯一 100% 确定**的成本项，因此它的真正价值是**同分时的决胜项（tie-breaker）**。

打分看两块：
1. **年持有成本（主体）= 管理费 + 托管费**，同类型横比，越低越高分。
   - 主动权益常见 1.2%–1.5%（管理）+ 0.2%（托管）；指数基金常见 0.15%–0.5%。**只在同类型内横比**——拿指数基金的低费率去压主动基金没有意义。
2. **交易摩擦（次要）= 申购费 + 赎回费（含 7 日惩罚档）**：对低换手、长期持有的筛选目标影响小，仅作提示；若策略会频繁申赎，则上调此项权重。

**费率分档**：同类型内年持有成本处于最低 1/3 → 80–100；中间 1/3 → 50–80；最高 1/3 → < 50。

---

## 辅助维度（不进主分，作打分微调与最终复核）

通过三维打分排出候选后，用辅助维度做最后一道"质检"，可对综合分做 ±5 的微调，但不改变主排序逻辑：

- **经理（`fund_manager_profile`）**：现任经理任职年限越长、且历史业绩确实属于他 → 业绩分可信度高；任职 1–2 年的"次新经理"业绩存疑 → 微调向下。一拖多（一人管十几只）也是减分信号。
- **风格漂移（`fund_style_analysis` / `get_fund_detail` 的 `size_style` / `invest_style`）**：基金宣称的风格与实际持仓暴露是否一致。挂"价值"实则追"成长"、行业暴露季季大变 → 业绩的"可复制性"打问号，微调向下。风格稳定、名实相符 → 微调向上。

辅助维度只做减分/质检，**不能把一只三维打分很低的基金靠"经理是明星"硬拉进来**——这正是追星接盘的典型错误。

---

## 输出范式

每只候选基金必须落到结构化结论，不能写"看着还行"：

- **出局名单**：基金代码 + 触发的门槛编号（"512xxx：门槛 3，规模 1.3 亿 < 2 亿，清盘风险"）。
- **入围打分表**：每只入围基金落 → `业绩分 / 规模分 / 费率分 → 综合分`，附一句话结论。
  - 例："011xxx：业绩 86（3y/5y 排名前 15%、夏普 1.2）/ 规模 78（48 亿，甜区）/ 费率 62（管理 1.5%，同类中游）→ 综合 79，长周期能力扎实、费率略高，决胜项不占优但整体入围 top 候选。"
- **排序**：综合分降序，给出最终推荐前 N 名 + 理由；同分用费率决胜。

**模糊判断 ≈ 没判**——必须落到分数和档位，不能停在定性形容词。

---

## 常见陷阱（筛选时反复自检）

1. **短期排名陷阱**：只看 `1m / 3m / 1y` 排名选基，等于追刚押对单一风格的基金，下一个风格周期大概率跑输。**长周期排名一致性 > 短期暴涨。**
2. **规模虚胖 / 规模诅咒**：新发或刚靠业绩吸金暴涨到几百亿的主动基，alpha 往往随规模衰减；也要警惕"明星基金封盘前最后一波"。规模看绝对值也看趋势。
3. **费率幻觉**：跨类型比费率（拿指数基金 0.15% 去否定主动基金 1.5%）是错的；费率只在同类型内横比，且权重本就最低，不要因为费率便宜就买进长期业绩差的基金。
4. **风格漂移**：历史业绩好可能来自一段押对的风格，而非可持续能力。名实不符、行业暴露漂移的基金，历史业绩的参考价值要打折。
5. **幸存者/经理变更**：历史业绩属于前任经理时，不能直接当现任的能力；换帅后等于"新基金"，回到观察位。

## 核心一句

基金筛选不是挑"过去一年涨最多的"，而是用**硬门槛剔除结构性风险**、再用**长周期风险调整后业绩为主（50%）、合理规模为辅（30%）、确定性费率作决胜（20%）**，把"运气好的短期黑马"和"能力扎实的长期优等生"区分开。

# 双创择时：regime 状态闸门 + 震荡市骨架 设计

适用 bot：**bot7、bot11**（两者同构：双创单指数择时，`仓位管理方法论` 都是「先识状态 → 只有趋势市骨架」）。
本次为**纯文本**改动（只改 `METHODOLOGY.md`），不动 world 数据层。

## 背景与问题

回测中观察到：agent **缺少"先判断当前是震荡市还是趋势市"这个环节，结果几乎全部按趋势打法操作**。具体表现两类：

1. 震荡区间里仍按趋势骨架追涨杀跌，被反复假突破甩进甩出。
2. 把估值视角的口号「趋势 > 估值」当成无方向的绝对律：在跳空暴跌、趋势破位、PE 打到历史极端低分位时，用「趋势>估值，趋势破位时估值低不构成逆势理由」把极端低估值这个信号**一票否决**。

## 根因（两层）

- **文本层**：`仓位管理方法论` 用「先识状态」立了趋势/震荡二分，却只展开「趋势市骨架」，震荡市只有一句话；识别震荡的硬标准（200日线走平+±10%+波动收敛）埋在「趋势视角」里，跟仓位管理分家，且「先识状态」是"问自己"式的软提示，不是强制前置闸门。`估值视角` line33 的「趋势 > 估值」措辞是单向规则（高估值+上升趋势→别因贵而减/拒入），但结尾甩出的口号被 agent 读成对称绝对律。
- **数据层**：每天注入 prompt 的指数块只有 MA5/MA20、约 20 天（`world/src/message.ts` indexBlock、`world/src/daily-context.ts` fetchIndexSnapshots），方法论要的 MA200/箱体/波动率信号 prompt 里没有。

**本次决定只修文本层**（用户选择）；数据层不在范围内。agent 仍需自行调 `market_index_quote` 取长窗口数据来执行判断。

## 设计原则（用户拍板的取舍）

- 震荡市骨架基调 = **防守型**：目标不是靠箱体高抛低吸赚 alpha，而是别让趋势打法在震荡里被假突破甩。降频、降信心、留子弹。
- 「趋势>估值」= **趋势优先，但不允许一票否决**：收成单向过滤器，禁止反向当"便宜也无视"的挡箭牌；极端读数仍是要认真对待的定性信号。
- regime 判断 = **有粘性的状态机**：跨日继承、确认才切换、慢于价格，绝不一天震荡一天趋势地翻（分类层 whipsaw = 既做错方向又付摩擦成本）。状态靠 mem0 跨日续。
- 结构 = **方案 A**：闸门 + 趋势/震荡双骨架并列，后续小节顺延重编号。
- 整合纪律：新增内容只放现在哪都没有的东西（突破交接 / 降频 / 成本闸门 / 状态粘性），不复述已有小节；重叠处用指针指回原节。

## 改动清单

四组改动。改动 1 / 2 / 3 / line47 在 bot7、bot11 两份文件中**正文完全相同**（仅小节编号不同）；改动 4 是各自的重编号 + 交叉引用修复。

---

### 改动 1 —「先识状态」改成强制前置闸门 + 状态粘性

**位置**：bot7「## 二、先识状态：趋势市 还是 震荡市」/ bot11「## 一、先识状态：趋势市 还是 震荡市」标题下的整段正文。

**替换原正文**（原文为"问自己…"开头到"箱底重仓买进、箱顶减乃至清空，做大波段高抛低吸"两条 bullet）为：

```
**每天第一步：先给市场状态分类，再选骨架。** 不分类、直接套趋势打法，是本框架最大的亏损源。每天只输出三选一：**趋势市 / 震荡市 / 过渡待定**。

问自己：现在指数是**"一个方向走得动、回调不破趋势"**（趋势市，动量有效），还是**"涨上去就回来、跌下去又弹、来回磨"**（震荡市，均值回归有效）？

- **识别标准**（按需调 market_index_quote 取长窗口自行判）：
  - **趋势市**：均线（如 MA60 / MA120）单边排列、斜率明显，价格沿一侧运行。
  - **震荡市**：200 日均线走平 + 价格在均线 ±10% 内来回 + 波动率收敛、箱体反复假突破。
  - **过渡待定**：信号不清、两边都不像 → 按防守处理（维持原状态 / 轻仓 / 观望）。
- 切错逻辑就是亏损来源：
  - **趋势市 → 顺势 / 时序动量**：方向对就拿住让 Beta 跑，方向转空就降仓乃至清空。别在趋势里高抛低吸（会被甩下车）。
  - **震荡市 → 防守、轻仓均值回归**：箱沿小幅低吸 / 减，绝不重仓赌箱体，放量突破即交接——细则见下节「震荡市骨架」。

**状态要有粘性——不是一天震荡、一天趋势地翻**（分类层自己 whipsaw 比不分类还糟：既做错方向又付摩擦成本）：
- **跨日继承**：每天先 mem0_search 取昨日的状态结论，**默认延续**；今天只回答"有没有出现确认级证据要改它"，没有就维持。
- **进入 / 切换都要确认，不看单日**：单日跳空、单根假突破、波动率瞬时放大**都不算**切换信号。切到趋势需放量有效突破且**站稳数日**；切到震荡需均线走平 + 箱体反复**持续数周**；任一未达确认 → 标「过渡待定」、维持原状态。
- **状态切换慢于价格，是特性不是缺陷**：宁可慢一拍跟上新趋势，也不在分类层来回翻。
- **落地**：每天 mem0_add 显式记下**当前状态标签 + 认定理由 + 推翻它需要看到什么**，让明天的你直接继承。
```

> 说明：原「震荡市 → 均值回归：箱底重仓买进、箱顶减乃至清空，做大波段高抛低吸」与防守型骨架冲突（重仓、大波段），故改成防守口径并指向新骨架节，同时消除与新节的重复。

---

### 改动 2 — 新增并列「震荡市骨架（防守型）」

**位置**：紧跟「趋势市骨架」节之后插入。编号：bot7 = **四**，bot11 = **三**（见改动 4）。

**新增整节**（`[N]` 按各文件填 四 / 三）：

```
## [N]、震荡市骨架（防守型）：少动、守箱沿、突破即交接

震荡市的目标**不是**靠箱体高抛低吸赚 alpha，而是**别让趋势打法在这里被反复假突破甩进甩出**。基调：降频、降信心、留子弹。

- **先划箱**：用「先识状态」认定的震荡区间——200 日线附近 ±10% + 近数月明显高低点定上下沿。划不出清晰箱体 = 不是可操作的震荡，按「过渡待定」观望。
- **箱体中段不动手**：只在贴近上 / 下沿时小幅动；中间来回的噪音一律不接。
- **轻仓、绝不重仓赌箱体**：双创箱体随时可能向上突破（=踏空）或向下跌穿（=接飞刀），所以震荡里压低单笔信心和总仓位——箱底小幅低吸、箱顶小幅减，量级远小于趋势市的大开大合。宁可仓位轻，不可方向赌死。
- **不追假突破**：均线在震荡里反复假突破，别每根假突破都按趋势骨架追进追出——这是震荡里最烧钱的错。
- **放量突破 → 立即交接给趋势市骨架（最关键）**：价格放量、有效突破箱体上沿或下沿**并站稳数日**，震荡假设作废 → 立刻切回趋势市骨架，按时序动量重定方向和仓位；**别在突破之后还反向 fade**。"站稳数日"才算交接——呼应先识状态的状态粘性，不被单日假突破骗翻。
- **成本闸门**：震荡里来回 round-trip 最容易被 7 日内 1.5% 早赎 + 申购费咬掉。箱体太窄、突破不明确时，正确动作往往是**不动**——等突破给方向，比在箱体里磨损强。
- 箱体有支撑的回踩可低吸、趋势破位的下跌不能接——区分见「不对称操作」节。
```

---

### 改动 3 —「趋势 > 估值」单向化 + 禁止一票否决

**位置**：`估值视角` 节末尾三段（原 line33-37，两份相同）。

**替换原三段**为：

```
**在上升趋势中，高估值绝不构成减仓或拒绝进场的理由——这才是"趋势 > 估值"的本意：它是一个单向过滤器，只否决"因为贵所以走 / 不进"，不是一句可以无视估值的通行证。** 择时主轴仍整个交给趋势 / 动量框架。

**别把它反过来用**：下跌 / 趋势破位时，"趋势 > 估值"只支持一件事——**不靠"便宜"去接飞刀**；它**不能**被拿来把极端估值读数一票否决。极端读数仍是要认真对待的定性信号。

所以估值在这里做两件事，都是**定性预警、不是择时扳机**：
- **极端泡沫预警（顶）**：出现全民进场信号（散户开户数创新高、新基金发行爆量、研报"非理性繁荣"）→ 当成"趋势可能见顶"的提示，配合趋势框架减仓。
- **周期反转预警（底）**：出现盈利见底、库存出清、需求拐点等**周期反转证据**（呼应核心认知·双创特性②）→ 记为**潜在筑底观察**（一个持久的观察态，写进 mem0 续到明天），**不立刻抄底**；但趋势一企稳就按骨架快速回补，**绝不因为"趋势还没好"就把这个信号丢掉**。注意：触发底部观察的是周期反转证据，不是单纯的低 PE 分位（分位对双创是噪音，见本节开头）。

成长性（营收/利润能否非线性增长、产业催化剂）只用来判断**这个标的值不值得在趋势里重仓**，不用来抄底或择时。
```

**附带 line47 收口**（`趋势视角` 节内，两份相同）：把硬识别标准移进闸门后，line47 收成指针，避免重复。

替换原句：
```
震荡市识别：200 日均线走平 + 指数在均线 ±10% 内 + 波动率收敛——均线信号会反复假突破，应转箱体思维而非趋势思维。
```
为：
```
震荡市里均线信号会反复假突破，要转箱体思维而非趋势思维——具体识别标准与状态切换纪律见下文「仓位管理方法论 · 先识状态」闸门。
```

---

### 改动 4 — 重编号 + 交叉引用修复

插入「震荡市骨架」后，其后小节顺延，并修内部引用。

**bot7**：
- 三、趋势市骨架（不变）
- **新增 四、震荡市骨架（防守型）**
- 四 → **五**、非对称操作
- 五 → **六**、止盈止损
- 六 → **七**、回撤控仓
- 七 → **八**、成本意识
- 交叉引用：回撤控仓节「再按**第四**条回补」→「再按**第五**条回补」（指非对称操作）。

**bot11**：
- 二、趋势市骨架（不变）
- **新增 三、震荡市骨架（防守型）**
- 三 → **四**、不对称操作
- 四 → **五**、止盈止损
- 五 → **六**、回撤控仓
- 六 → **七**、成本意识
- 七 → **八**、实战判断：成对矛盾的格言
- 交叉引用：
  - 唯一信仰节「不对称操作…**见三**」→「**见四**」；「前景理论反操作，**见四**」→「**见五**」；「回撤优先，**见五**」→「**见六**」。
  - 回撤控仓节「再按**第三**条回补」→「再按**第四**条回补」（指不对称操作）。

## 验收标准

1. bot7、bot11 各自 `仓位管理方法论` 出现「先识状态」闸门（含三态分类 + 状态粘性四条）、「趋势市骨架」「震荡市骨架（防守型）」两节并列。
2. 「先识状态」震荡分支不再写"重仓/大波段高抛低吸"，改为防守口径并指向震荡骨架节。
3. 「估值视角」末段「趋势>估值」为单向表述 + 含"禁止一票否决" + "周期反转→筑底观察"两件事；line47 收成指针。
4. 两份文件的小节编号连续无重复，且所有「第 X 条 / 见 X」交叉引用指向正确小节。
5. 仅改 `bots/bot7/METHODOLOGY.md`、`bots/bot11/METHODOLOGY.md`，不动 world 代码。

## 不做（YAGNI / 范围外）

- 不改 world 数据层（不在 daily prompt 注入 MA60/120/200、ATR%、箱体上下沿）。
- 不改 bot1（红利）、bot2（产能周期）——它们是另一套框架。
- 不引入硬阈值数字（持仓天数、固定止损线等）——保持本框架"定性原则、不用固定数字"的一贯口径。
